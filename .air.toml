# .air.toml — Air config for hot reload
root = "/app"
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/foody ./cmd/foody/cmd/*.go"
bin = "tmp/foody"
full_bin = ""
include_ext = ["go"]
exclude_dir = ["tmp", "vendor"]

[log]
timestamp = true

